generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

model AccessLog {
  id        String   @id @default(uuid())
  device    String
  auth      Boolean
  createdAt DateTime @default(now())

  @@map("access_logs")
}